@model RollCall.Models.EmployeeViewModel

@{
    ViewBag.Title = "Create";
    Layout = "~/Views/Shared/_Layout.cshtml";
}

<h2>新增員工</h2>

@using (Html.BeginForm())
{
    <div class="form-horizontal">
        @Html.AntiForgeryToken()


        <div class="form-group">
            @Html.Label("姓名", htmlAttributes: new { @class = "control-label col-md-2" })
            <div class="col-md-10">
                @Html.EditorFor(model => model.EmpName, new { htmlAttributes = new { @class = "form-control" } })
                @Html.ValidationMessageFor(model => model.EmpName, "", new { @class = "text-danger" })
            </div>
        </div>

        <div class="form-group">
            @Html.Label("航線", htmlAttributes: new { @class = "control-label col-md-2" })
            <div class="col-md-10">
                @Html.EditorFor(model => model.Department, new { htmlAttributes = new { @class = "form-control" } })
                @Html.ValidationMessageFor(model => model.Department, "", new { @class = "text-danger" })
            </div>
        </div>

        <div class="form-group">
            @Html.Label("職務", htmlAttributes: new { @class = "control-label col-md-2" })
            <div class="col-md-10">
                <!-- 下拉選單 -->
                @*@Html.DropDownListFor(model => model.Position, ViewBag.Positions as SelectList, "請選擇職位", new { id = "Position" })*@

                <!-- 新職務輸入框 -->
                @Html.TextBoxFor(model => model.Position, new { @class = "form-control", id = "NewPosition" })

                <!-- 隱藏欄位，最終提交的職務值 -->
                @Html.HiddenFor(model => model.Position, new { id = "FinalPosition" })
            </div>
        </div>

        <div class="form-group">
            @Html.Label("員工編號", htmlAttributes: new { @class = "control-label col-md-2" })
            <div class="col-md-10">
                @Html.EditorFor(model => model.EmpNumber, new { htmlAttributes = new { @class = "form-control" } })
                @Html.ValidationMessageFor(model => model.EmpNumber, "", new { @class = "text-danger" })
            </div>
        </div>

        <div class="form-group">
            @Html.Label("業務所屬部門", htmlAttributes: new { @class = "control-label col-md-2" })
            <div class="col-md-10">
                <select id="SalesDepID" name="SalesDepID" class="form-control">
                    <option value="">請選擇</option>
                    <option value="PS.TPE">PS.TPE</option>
                    <option value="PC.TPE">PC.TPE</option>
                    <option value="PS.TXG">PS.TXG</option>
                    <option value="PC.TXG">PC.TXG</option>
                    <option value="PS.KHH">PS.KHH</option>
                    <option value="PC.KHH">PC.KHH</option>
                </select>
                @Html.ValidationMessageFor(model => model.SalesDepID, "", new { @class = "text-danger" })
            </div>
        </div>


        <div class="form-group">
            <div class="col-md-10">
                <button type="button" class="btn btn-success" onclick="createEmployee()">新增員工</button>
            </div>
        </div>
    </div>
}

<div>
    @Html.ActionLink("返回員工資料頁面", "Index")
</div>
<script>
    // 當輸入新的職務名稱時，更新隱藏欄位的值
    document.querySelector("form").addEventListener("submit", function () {
        var newPosition = document.getElementById("NewPosition").value;
        //var positionDropdown = document.getElementById("Position").value;
        var finalPosition = document.getElementById("FinalPosition");

        if (newPosition.trim() !== "") {
            finalPosition.value = newPosition; // 使用輸入框的值
        } else {
            finalPosition.value = positionDropdown; // 使用下拉選單的值
        }
    });

    // 當選擇下拉選單時，更新隱藏欄位的值並清空輸入框
    //document.getElementById("Position").addEventListener("change", function () {
    //    var positionDropdown = this.value;
    //    var finalPosition = document.getElementById("FinalPosition");

    //    if (positionDropdown.trim() !== "") {
    //        finalPosition.value = positionDropdown; // 使用下拉選單的值
    //        document.getElementById("NewPosition").value = ""; // 清空輸入框
    //    }
    //});

    // 當輸入框有輸入時，清空下拉選單的值
    document.getElementById("NewPosition").addEventListener("input", function () {
        var newPosition = this.value;
        var positionDropdown = document.getElementById("Position");
        var finalPosition = document.getElementById("FinalPosition");

        if (newPosition.trim() !== "") {
            //positionDropdown.value = ""; // 清空下拉選單的值
            finalPosition.value = newPosition; // 使用輸入框的值
        }
    });

    function createEmployee() {
        const empName = document.querySelector('[name="EmpName"]').value.trim();
        const department = document.querySelector('[name="Department"]').value.trim();
        const empNumber = document.querySelector('[name="EmpNumber"]').value.trim();
        const salesDepId = document.getElementById("SalesDepID").value.trim();
        const newPosition = document.getElementById("NewPosition").value.trim();
        const finalPosition = newPosition !== "" ? newPosition : "";

        // 檢查是否有未填入的欄位
        if (!empName) {
            alert("請填寫員工姓名！");
            return;
        }
        if (!department) {
            alert("請填寫部門！");
            return;
        }
        if (!finalPosition) {
            alert("請填寫職位！");
            return;
        }
        if (!empNumber) {
            alert("請填寫員工編號！");
            return;
        }
        if (!salesDepId) {
            alert("請選擇業務所屬部門！");
            return;
        }

        // 組裝資料
        const data = new URLSearchParams({
            EmpName: empName,
            Department: department,
            Position: finalPosition,
            EmpNumber: empNumber,
            SalesDepID: salesDepId
        });

        // 發送請求
        fetch('/RCS_EMPLOYEES/Create', {
            method: 'POST',
            headers: {
                'Content-Type': 'application/x-www-form-urlencoded',
                'RequestVerificationToken': $('input[name="__RequestVerificationToken"]').val()
            },
            body: data
        })
            .then(response => response.json())
            .then(result => {
                if (result.success) {
                    alert('員工新增成功！');
                    window.location.href = '/RCS_EMPLOYEES/Index';
                } else {
                    alert(result.error || '新增失敗！');
                }
            })
            .catch(error => {
                console.error('新增失敗:', error);
                alert('新增過程中發生錯誤，請稍後再試！');
            });
    }

</script>